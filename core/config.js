'use strict';

/**
 * Central configuration placeholder.
 * Phase 1: scaffold only, no behavior changes.
 */

export const config = {
  schemaVersion: 1,
  features: {
    // e.g., detectors: true, protections: true
  },
};
