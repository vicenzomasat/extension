'use strict';

/**
 * Instagram-specific overrides (stub).
 * Phase 1: scaffold only, no behavior changes.
 */

export function applyInstagramOverrides(/* context */) {
  // Placeholder for Instagram specific tweaks.
}
