'use strict';

/**
 * Default site overrides (stub).
 * Phase 1: scaffold only, no behavior changes.
 */

export function applyDefaultOverrides(/* context */) {
  // Placeholder for generic site tweaks.
}
