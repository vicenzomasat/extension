'use strict';

/**
 * TikTok-specific overrides (stub).
 * Phase 1: scaffold only, no behavior changes.
 */

export function applyTikTokOverrides(/* context */) {
  // Placeholder for TikTok specific tweaks.
}
