'use strict';

/**
 * YouTube-specific overrides (stub).
 * Phase 1: scaffold only, no behavior changes.
 */

export function applyYouTubeOverrides(/* context */) {
  // Placeholder for YouTube specific tweaks.
}
